const fs = require('fs');
const readline = require('readline');
// Let's modify agent.js temporarily to log different RR windows.
// Actually, it's easier to write a minimal script to calculate it locally.
